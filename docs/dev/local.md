## Local dev environment

### Using [Lima](https://lima-vm.io/) on M1 MacBook Pro

1. Create a Linux VM

- Currently using Lima Fedora instance

1. Prepare the VM as a `kakuri` node
2. Run `kakuri-agent` on the VM
3. Run `kakuri-control-plane` locally
